import React, { useState } from 'react'
interface PropsI {
    closeModal: () => void;
}
const PredictionModalForm = ({ closeModal }: PropsI) => {

    return (
        <div>PredictionModalForm</div>
    )
}

export default PredictionModalForm